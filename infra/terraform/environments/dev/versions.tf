terraform {
  required_version = ">= 1.6.0"

  required_providers {
    oci = {
      source  = "oracle/oci"
      version = "~> 6.0"
    }
  }

  # Configure this after the Object Storage state bucket exists.
  # backend "s3" {}
}

