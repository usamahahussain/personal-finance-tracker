# Production Terraform

Use the `dev` environment as the starting point once the initial infrastructure shape is proven.

Before creating production:

- Configure remote state.
- Decide whether to keep the public VM model or introduce a Load Balancer.
- Confirm backup, restore, and secret rotation practices.

