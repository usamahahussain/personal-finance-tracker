# AGENTS.md
## Repository expectations
- No changes should be made to the FastAPI backend, all required changes should be communicated to the user for manual creation
- All changes to the NextJS frontends should be handled by you
- SSH to the VM is accessible using the command: `ssh -i /Users/usamahhussain/git/sandpit/personal-finance-tracker/keys/pft_dev_vm opc@79.72.70.38`
- There will already be a user-instantiated `npm run dev` and `fastapi dev` session running on the VM respectively so you can directly test committed code on the VM using local curl
- This is a personal project for tracking finances and budgeting using a similar style to LunchMoney and other personal finance applications
